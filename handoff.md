# 人生决策器（Decide Better）项目交接文档

更新日期：2026-09-02

## 1. 当前任务

本项目旨在把《行为贝叶斯》中适用于关键决策的核心思想，整理为一个可开源、可复用、可跨智能体加载的 Agent Skill。它不蒸馏或复述整本书，也不代替用户拍板，而是帮助用户在现实不确定性下：

- 从基础率和竞争假设出发校准证据；
- 判断新信息是否真实、独立且足以改变结论；
- 用系统思维检查反馈、延迟、阈值和二阶影响；
- 对信息按功能分层，避免把热点信号误当长期规律；
- 比较最佳可行替代方案与机会成本；
- 检查最坏合理情景、承受力、可逆性和选择权；
- 输出条件式建议、置信度、最小下一步和结论改变条件。

当前开发阶段已告一段落。公开版本为 `v0.1.1`，核心功能已经完成，后续工作以真实平台验证、评测体系和渐进式优化为主。

## 2. 当前真实状态

### 仓库与版本

- 中文名：人生决策器
- Skill 名称／仓库名：`decide-better`
- 默认分支：`main`
- 当前提交：`cdd9cd5 feat: add cross-agent compatibility guidance`
- 远程仓库：`https://github.com/ruizhihu/decide-better.git`
- 远程名称：`origin`
- GitHub 可见性：Public
- 已发布版本：
  - `v0.1.0`：首个可用版本
  - `v0.1.1`：加入跨智能体兼容说明，当前 Latest Release
- Git 提交身份仅配置在本仓库，署名为 `ruizhihu`，使用 GitHub 隐私邮箱。

### 本地位置与安装方式

- 真实项目及 Codex 安装目录：`~/.codex/skills/decide-better`
- 桌面入口：`~/Desktop/decide-better`
- 桌面入口当前是指向真实项目目录的软链接，不是第二份项目副本。
- 因真实 Git 仓库本身就在 Codex Skills 目录中，对该目录的修改会直接成为 Codex 使用的版本，也能继续正常提交和推送。
- 删除桌面软链接不会删除项目；不要误删 `~/.codex/skills/decide-better`，它才是真实目录。

### 当前文件

- `SKILL.md`：跨平台核心行为指令，是唯一行为源。
- `references/deep-analysis.md`：仅在重要、高不确定或高风险决策中读取的深度分析参考。
- `agents/openai.yaml`：Codex 专用显示名称、默认提示和显性调用配置。
- `README.md`：项目介绍、功能、安装、用法和输入输出示例。
- `COMPATIBILITY.md`：跨平台接入原则、结构兼容性、真实运行边界和测试标准。
- `LICENSE`：MIT License，署名 `ruizhihu`。
- `handoff.md`：本交接文档。

项目有意不包含运行脚本、构建系统或第三方依赖；核心交付物是指令文件和参考文件。

## 3. 已经完成的内容

### 3.1 决策方法提炼

已结合用户上传的书籍片段完成四组核心思想的提炼与整合：

1. 贝叶斯式决策：先验、基础率、竞争假设、证据更新和避免虚假精确。
2. 新信息验证：真实性、判别力、增量性、稳健性和决策相关性五道门。
3. 系统思维：反馈回路、时滞、阈值、相关风险、利益相关者反应和二阶后果。
4. 信息分层与机会成本：基础规律、分析洞察、实时信号，以及直接、隐性和不确定性成本。

这些思想已被转化为可执行的对话流程，而不是书籍摘要。原始书籍图片没有进入公开仓库，避免公开版权材料和本地隐私内容。

### 3.2 Skill 行为设计

已实现以下核心行为：

- 只询问可能改变建议的关键信息；信息不可得时，以显式假设或条件分支继续。
- 默认快速模式，高重要性、高不确定、难以逆转或严重下行风险时进入深度模式。
- 明确区分事实、解释、假设、预测和价值偏好。
- 检查基础率、反例、幸存者偏差、共同来源和证据独立性。
- 概率输入可靠时才做数值贝叶斯更新；否则使用定性证据强度。
- 把机会成本定义为“放弃的最佳可行替代方案”，避免把所有未选方案相加。
- 把生存与恢复能力设为硬约束，同时避免把“亏得起”误判为“值得做”。
- 优先设计试点、分阶段投入、上限、缓冲、检查点、退出路径和止损条件。
- 固定输出决策框架、条件式建议、置信度、关键证据与假设、机会成本与下行风险、最小下一步和更新计划。
- 医疗、法律和投资等高风险领域仅作辅助分析，并提示专业复核。
- 未经单独授权，不执行购买、交易、联系他人或提交信息等外部行动。

### 3.3 官方结构与文档

- 使用官方 Skill 初始化流程创建项目结构。
- 完成带 YAML frontmatter 的 `SKILL.md`。
- 完成 `agents/openai.yaml`，显示名为“人生决策器”；当前配置为仅允许通过 `$decide-better` 显性调用。
- 完成简洁、与真实功能一致的 `README.md`。
- 添加 MIT License。
- 未添加不必要的脚本。

### 3.4 测试与验证

- 初始开发阶段已用不同角色和场景模拟重要决策，覆盖职业选择、个人重大选择和业务决策等类型。
- 测试重点包括：是否先问关键问题、是否给出条件式建议、是否比较机会成本、是否检查最坏合理情景、是否避免编造概率，以及是否保留用户最终决定权。
- 已完成敏感信息和公开内容检查，并按建议移除或改写个人隐私、本地绝对路径和不应公开的信息。
- 当前 `SKILL.md` 与 Codex 安装目录中的文件完全一致。
- 本轮尝试再次运行官方 `quick_validate.py` 时，系统 Python 和 Codex 内置 Python 均缺少 `PyYAML`，报错 `ModuleNotFoundError: No module named 'yaml'`。这是验证运行环境缺少依赖，不是当前 Skill 已知的格式错误；详见“卡住的问题”。

### 3.5 跨平台兼容更新

`v0.1.1` 已把 `SKILL.md` 定义为可移植的唯一行为源，并明确：

- `agents/openai.yaml` 仅供 Codex 使用，其他平台可以忽略。
- 核心指令不依赖特定厂商的工具名、UI 或自动调用机制。
- 其他平台可通过原生 Skill、项目规则、自定义指令或附件加载。
- 深度模式需要保留 `references/deep-analysis.md`；平台无法读取关联文件时，应明确能力边界。

兼容性结论：

- Claude Code、Cursor、Gemini CLI：公开格式与本项目结构兼容，但本机没有安装客户端，尚未做真实运行。
- WorkBuddy、豆包工作／豆包：没有确认公开的第三方 `SKILL.md` 原生导入规范，目前只承诺项目指令或附件适配。
- DeepSeek：可作为系统提示或附件使用，也可作为其他 Agent 的模型后端；未发现统一的原生 Skill 加载器规范。

“结构兼容”与“真实运行通过”已在 `COMPATIBILITY.md` 中明确分开，避免过度宣称。

### 3.6 Git 与 GitHub 发布

- 已安装并配置 Git。
- 已初始化仓库，默认分支为 `main`。
- 已完成首次提交并使用 GitHub 隐私邮箱关联作者身份。
- 已安装并登录 GitHub CLI。
- 已创建公开 GitHub 仓库并配置 `origin`。
- 已推送 `main`。
- 已创建并发布 `v0.1.0` 与 `v0.1.1` Tag 和 GitHub Release。
- 当前本地 `main` 与 `origin/main` 一致。

## 4. 卡住的问题与当前风险

### 4.1 Codex 对话历史索引故障

本次长对话的原始消息没有丢失，但 Codex 界面的历史投影数据库卡住，导致近一段时间的消息“写入原始会话文件，却不显示在界面”。

已确认：

- 故障仅涉及本地 Codex 任务；内部任务标识不写入公开仓库。
- 原始 JSONL 会话仍完整存在并持续追加。
- 历史索引卡在字节偏移 `47607447`、序号 `1164`。
- 日志错误为：`expected ordinal 1164, got 1157`。
- 故障附近的工具调用和输出全部成对，原始会话结构没有发现缺失。
- 已在本机恢复目录中备份原始会话、历史数据库和状态数据库；精确路径不写入公开仓库。

已进行过带退出检查、游标检查、完整性验证和预先备份的本地索引修复；界面恢复结果仍需在本机确认。

不要删除原始 JSONL、整个 `~/.codex` 目录或该任务。若修复失败，应先恢复备份或导出缺失消息，再考虑创建新任务继续。

### 4.2 `quick_validate.py` 当前无法复跑

官方验证脚本依赖 `PyYAML`，但以下两个解释器都没有 `yaml` 模块：

- 系统 `python3`
- Codex bundled Python

不要为了通过验证直接污染系统 Python。推荐后续在临时虚拟环境中安装 `PyYAML`，运行官方脚本后删除虚拟环境，或使用官方已经包含该依赖的验证运行时。

### 4.3 跨平台仍缺真实运行测试

当前兼容结论主要基于公开规范和文件结构。Claude Code、Cursor、Gemini CLI、WorkBuddy、豆包与 DeepSeek 目标环境尚未逐一实际加载并完成同一决策测试。发布说明中不得把“结构兼容”写成“所有平台均已实测通过”。

### 4.4 `.DS_Store` 防护

已在根目录添加 `.gitignore`，忽略所有层级的 `.DS_Store`，并清理当时存在的本地临时文件。后续提交前仍应检查是否出现其他系统文件或测试垃圾。

### 4.5 桌面软链接导致 Codex 沙箱路径异常

虽然桌面软链接方便访问，但当 Codex 项目以 `~/Desktop/decide-better` 作为可写工作区根目录时，执行环境会报“writable root contains symlink component”，从而阻止终端命令、浏览器运行时或文件写入。

后续在 Codex 中应把项目根目录设置为真实路径：

```text
~/.codex/skills/decide-better
```

桌面软链接可保留作 Finder 入口，但不要把它作为 Codex 的工作区根目录。

## 5. 下一步计划

### 优先级 P0：完成恢复与环境稳定

1. 在本机确认原始 JSONL 和恢复备份仍完整可读。
2. 重新打开 Codex，检查本任务缺失历史是否重新出现。
3. 若仍未恢复，停止在故障任务继续写入，保留最新备份并把缺失消息导出为 Markdown。
4. 把 Codex 保存项目的根目录设置为真实目录 `~/.codex/skills/decide-better`，避免桌面软链接触发沙箱错误。

### 优先级 P1：仓库清理与验证

1. 保持 `.gitignore` 对 `.DS_Store` 的全层级忽略规则。
2. 每次公开发布前检查临时文件、隐私内容和本地绝对路径。
3. 创建临时 Python 虚拟环境，安装 `PyYAML`，重新运行官方 `quick_validate.py`。
4. 检查 `git status`，确认只包含预期变更。
5. 提交前审阅差异；推送和发布新版本仍需用户确认。

### 优先级 P2：跨平台真实测试

使用 `COMPATIBILITY.md` 中统一的工作选择测试请求，逐平台记录：

- Skill 是否能被自动发现或手动加载；
- 能否按需读取 `references/deep-analysis.md`；
- 是否只询问会改变建议的信息；
- 输出是否包含条件式建议、置信度、机会成本、最坏合理情景、最小下一步和更新条件；
- 是否避免虚构来源、概率和外部行动；
- 实际平台版本、加载路径、限制和测试日期。

建议先测试 Claude Code、Cursor 和 Gemini CLI，再测试只能通过项目指令或附件适配的平台。

### 优先级 P3：下一版本候选

可考虑在 `v0.2.0` 中加入：

- 一组不含隐私、可重复执行的评测案例和通过标准；
- 中英文完整文档，而不仅是英文 quick start；
- 更清晰的快速模式与深度模式示例；
- 对团队决策、创业投资、职业迁移等常见场景的示例，但避免把场景模板变成僵化问卷；
- 对结论更新、复盘日期和止损触发器的更强约束；
- 真实跨平台测试矩阵。

除非确有重复性自动化需求，否则继续保持项目无运行脚本、低依赖和易移植。

## 6. 踩过的坑与经验

### Skill 设计

- 不要把贝叶斯思维简化成“给所有事情算概率”；不可辩护的数字会制造虚假精确。
- 新信息的价值不等于信息量。重复报道、同源证据和生动案例可能几乎没有增量价值。
- 成功概率最高的方案不一定最值得选，还要比较收益、机会成本、下行风险、承受力和可逆性。
- 系统图中的每条因果箭头都只是待验证假设，不能把复杂图形当作证据。
- 信息分层是功能分工，不是可信度排名；实时信号并不天然高于基础规律。
- 决策分析要服务下一步行动，不能把框架堆叠成新的拖延方式。

### 跨平台兼容

- 可移植的核心应放在 `SKILL.md`，厂商专用元数据应保持可选。
- 不要为没有公开规范的平台猜测目录或伪造配置文件。
- “格式兼容”“能被发现”“能读取参考文件”“真实运行符合预期”是四个不同层次，必须分别验证。
- 平台只能接受单段提示时，优先保留完整 `SKILL.md`；深度参考按需补充。

### 本地安装与路径

- “项目副本软链接到安装目录”和“安装目录是真实项目、桌面是反向软链接”是两种不同结构，必须先用 `ls -ld` 与 `readlink` 确认，不能凭路径外观判断。
- 当前真实仓库位于 `~/.codex/skills/decide-better`，因此桌面软链接可删，但真实目录不可删。
- Codex 沙箱不接受包含软链接组件的可写根目录；项目配置必须指向真实路径。

### Git 与 GitHub

- Git 提交邮箱不是 GitHub 登录账号。开源仓库应优先使用 GitHub 提供的隐私邮箱。
- GitHub 网页已登录不等于 GitHub CLI 已登录；必须以 `gh auth status` 为准。
- 浏览器设备授权码、Token、密码和二次验证码都不应发送到对话或写入仓库。
- 创建公开仓库前应再次检查 Token、个人隐私、本地绝对路径、临时文件和测试垃圾。
- Tag、Release 与普通提交是不同对象；发布前应确认目标提交、Tag 和 Release 标题一致。

### 对话与恢复

- Codex 界面不显示消息，不代表原始会话文件已经丢失；应先检查任务记录、归档状态、JSONL 和投影数据库。
- 在应用仍运行时不要直接修改 SQLite 历史数据库。
- 任何索引修复前都应同时备份原始会话、历史数据库和状态数据库，并校验完整性。
- 遇到索引卡死时继续在同一任务写入，会造成“消息保存但界面不可见”的错觉；应尽快完成恢复或切换到新任务。

## 7. 接手检查清单

接手后建议依次执行：

1. 确认当前目录为 `~/.codex/skills/decide-better`，不是桌面软链接路径。
2. 运行 `git status --short --branch`，确认当前分支与未提交文件。
3. 确认 `git log -1` 仍以 `cdd9cd5` 或其后继提交为 HEAD。
4. 确认 `origin` 仍指向 `https://github.com/ruizhihu/decide-better.git`。
5. 确认 Codex 能发现 `$decide-better`，并执行一个快速决策用例。
6. 处理 `.DS_Store` 和 `.gitignore`。
7. 在隔离环境中复跑 `quick_validate.py`。
8. 核实对话历史修复是否成功；失败时从恢复备份继续，不要删除原始会话。
9. 任何新的提交、推送、Tag 或 Release 都应由用户再次确认。

## 8. 当前交接结论

`decide-better` 已达到可公开使用的 `v0.1.1` 状态：核心决策流程、Codex 元数据、深度分析参考、README、MIT License、跨平台说明、GitHub 仓库与 Releases 均已完成。当前主要技术债不是 Skill 核心功能，而是对话历史索引恢复、验证环境缺少 `PyYAML`、桌面软链接引发的 Codex 沙箱问题，以及跨平台尚未完成真实运行测试。

后续应优先稳定本地开发与恢复环境，再通过可重复评测推进 `v0.2.0`，避免在没有真实测试证据时继续扩张兼容性宣称。
